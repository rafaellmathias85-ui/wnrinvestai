/**
 * calendario.js
 * simulador.js
 * diario.js
 * -----------
 * These modules are implemented inside oportunidades.js for
 * convenience during the first iteration.
 *
 * When the project grows, move each section to its own file:
 *
 *   js/calendario.js  → renderCalendario(), calIA()
 *   js/simulador.js   → renderSimulador(), simUpdate(), simIA()
 *   js/diario.js      → renderDiario(), toggleDiaryForm(),
 *                        saveDiaryEntry(), avaliarDecisao()
 *
 * Remember to add the new <script> tags to index.html and
 * remove the corresponding sections from oportunidades.js.
 */
